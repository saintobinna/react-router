import { fromik, field, form } from "formik";
import React from "react";

const AddMovie = () => {
  return (
    <div
      style={{
        margin: "50px",
        borde: "1px solid break",
        borderradius: "5px",
        padding: "10px",
      }}
    >
      <h1>Add Movie</h1>
    </div>
  );
};

export default AddMovie;
